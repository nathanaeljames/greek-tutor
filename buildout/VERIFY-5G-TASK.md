# VERIFY-5G-TASK.md — produce VERIFY-5G.md (no code, no commits, no pushes)

You are the round winner (Opus 5), after applying 5G-XPATCH1. Your sole
task is to author `buildout/VERIFY-5G.md` for Nathanael's device pass.
Write the file and stop.

## The bar for this document

VERIFY-5F was too long and asked for things automation had already
settled or the implementer could have decided alone. Do not repeat
that. VERIFY-5G contains ONLY items that require Nathanael personally:
a human ear on a clip, eyes on DOSBox, a real-iPhone behavior, or a
product decision. Nothing that ui-behavior, ui-walk, ui-modals,
ui-offline or check:shapes can settle belongs here — those 849+
assertions are the record for everything mechanical. If you find
yourself writing an item you could answer by running a script or
reading the data, answer it yourself and leave it out.

Target length: the nine spec-authored items, the two build-raised
items, and the airplane-mode pass. Roughly a dozen items, one page
each side of the decision. No preamble essays.

## Required content

**The nine spec items (a)-(i)**, each restated in one or two lines with
what the port CURRENTLY does, so a verdict has something to land
against:

- (a) `chapt_9_i_mpar` vs the ch9 Passive chart's Say Paradigm in
  DOSBox — same clip or different?
- (b) `chapt_9_i_voc11` — confirm it says dierchomai.
- (c) `chapt_10_l_eimi` — confirm it is a standalone eimi suited to
  the topic-title tap.
- (d) Repeat This Exercise semantics (D-42). LEAD with the §5.4
  finding: neither rail-walk panel draws the checkbox — both show only
  "With Accents" on page one of a paged entry surface. So the first
  question is whether the control exists in DOSBox at all (check page
  two). If it does not, the answer retires D-42 and the modeled
  semantics come out; if it does, describe what checking it and
  finishing a verse actually does.
- (e) Objective 6 says "Jn 6:23b"; the verse is Rom 6:23b. Keep the
  original's typo or fix it? (Divergence entry either way.)
- (f) ἔρχομαι glossed "I go in, enter" (copy of εἰσέρχομαι's gloss).
  Keep verbatim or fix?
- (g) Does the original's ch10 parser accept a VOICE answer for
  present-tense εἰμί items, and which? (The port derives Active for
  present, Middle for future.)
- (h) Does the original's ch10 parsing Hint cycle beyond the FA and FM
  charts? (The port wires FA+FM only.)
- (i) `j_TvD2` and `j_palp` — confirm both are genuinely orphaned
  (referenced by nothing).

**The two build-raised items:**

- (j) Irregular Futures headers: spec says underlined, panel p6-2
  shows not underlined; the port underlines per §0 spec-wins. Keep or
  fix.
- (k) ch9's SM speller shows Previous Page / Next Page in the original
  (panel p13-2); the port has been a single-field speller since
  chapter 3. Confirm the standing divergence is still acceptable.

**Device half of directive 4:** airplane-mode walk of both chapters
after downloading each pack through the app, including at least one
ch10 Translation Drill tap (the surface behind the 31-id case fix) and
one Repeat-checkbox exercise.

## Format

One checkbox per item, PASS/FAIL or a decision line, and a short notes
field. Group: audio listens first (a, b, c, i), then DOSBox
observations (d, g, h, j, k), then decisions (e, f), then the airplane
pass. State the port's current behavior with each item — never make
Nathanael look it up.

Do NOT include: rendering confirmation, overflow, tap-target color,
popup reachability, commit-order mechanics, timing measurements, or
anything else the harness already pins.
